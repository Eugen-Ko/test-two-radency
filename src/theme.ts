import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#eeeeee",
      dark: "#999999",
      contrastText: "#212121",
    },
    secondary: {
      main: "#212121",
      light: "#ffffff",
    },
  },
});
